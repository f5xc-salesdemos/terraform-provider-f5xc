---
page_title: "f5xc_segment Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Shape of the segment specification
---

# f5xc_segment (Resource)

Shape of the segment specification

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Segment Resource Example
# Shape of the segment specification

# Basic Segment configuration
resource "f5xc_segment" "example" {
  name      = "my-segment"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # Empty. This can be used for messages where no values are ...
    disable {
      # Configure disable settings
    }
    # Empty. This can be used for messages where no values are ...
    enable {
      # Configure enable settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the Segment. Must be unique within the namespace.
- `namespace` (String) Namespace where the Segment will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `disable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disable))
- `enable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--enable))
- `labels` (Map of String) Labels to apply to this resource.

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--disable"></a>
### Nested Schema for `disable`


<a id="nestedblock--enable"></a>
### Nested Schema for `enable`

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_segment.example system/example
```
