---
page_title: "f5xc_dc_cluster_group Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Create DC Cluster group in given namespace
---

# f5xc_dc_cluster_group (Resource)

Create DC Cluster group in given namespace

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Dc Cluster Group Resource Example
# Create DC Cluster group in given namespace

# Basic Dc Cluster Group configuration
resource "f5xc_dc_cluster_group" "example" {
  name      = "my-dc-cluster-group"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # DC Cluster Group Mesh Type. Details of DC Cluster Group M...
    type {
      # Configure type settings
    }
    # Empty. This can be used for messages where no values are ...
    control_and_data_plane_mesh {
      # Configure control_and_data_plane_mesh settings
    }
    # Empty. This can be used for messages where no values are ...
    data_plane_mesh {
      # Configure data_plane_mesh settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the DcClusterGroup. Must be unique within the namespace.
- `namespace` (String) Namespace where the DcClusterGroup will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `labels` (Map of String) Labels to apply to this resource.
- `type` (Block, Optional) DC Cluster Group Mesh Type. Details of DC Cluster Group Mesh Type (see [below for nested schema](#nestedblock--type))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--type"></a>
### Nested Schema for `type`

Optional:

- `control_and_data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--type--control_and_data_plane_mesh))
- `data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--type--data_plane_mesh))

<a id="nestedblock--type--control_and_data_plane_mesh"></a>
### Nested Schema for `type.control_and_data_plane_mesh`


<a id="nestedblock--type--data_plane_mesh"></a>
### Nested Schema for `type.data_plane_mesh`

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_dc_cluster_group.example system/example
```
