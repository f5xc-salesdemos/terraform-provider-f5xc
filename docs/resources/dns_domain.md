---
page_title: "f5xc_dns_domain Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Create DNS Domain in a given namespace. If one already exist it will give a error.
---

# f5xc_dns_domain (Resource)

Create DNS Domain in a given namespace. If one already exist it will give a error.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Dns Domain Resource Example
# Create DNS Domain in a given namespace. If one already exist it will give a error.

# Basic Dns Domain configuration
resource "f5xc_dns_domain" "example" {
  name      = "my-dns-domain"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # Empty. This can be used for messages where no values are ...
    volterra_managed {
      # Configure volterra_managed settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the DNSDomain. Must be unique within the namespace.
- `namespace` (String) Namespace where the DNSDomain will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `dnssec_mode` (String) DNSSEC Mode. Enable or disable DNSSEC on the DNS Domain DNSSEC is disabled DNSSEC is enabled
- `labels` (Map of String) Labels to apply to this resource.
- `volterra_managed` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--volterra_managed))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--volterra_managed"></a>
### Nested Schema for `volterra_managed`

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_dns_domain.example system/example
```
