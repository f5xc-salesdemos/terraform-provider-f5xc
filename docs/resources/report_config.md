---
page_title: "f5xc_report_config Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Report configuration is used to schedule report generation at a later point in time.
---

# f5xc_report_config (Resource)

Report configuration is used to schedule report generation at a later point in time.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Report Config Resource Example
# Report configuration is used to schedule report generation at a later point in time.

# Basic Report Config configuration
resource "f5xc_report_config" "example" {
  name      = "my-report-config"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # Report recipients. Report recipients
    report_recipients {
      # Configure report_recipients settings
    }
    # User Groups. Select one or more user groups, to which the...
    user_groups {
      # Configure user_groups settings
    }
    # Report Type Waap. Report Type Waap
    waap {
      # Configure waap settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the ReportConfig. Must be unique within the namespace.
- `namespace` (String) Namespace where the ReportConfig will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `labels` (Map of String) Labels to apply to this resource.
- `report_recipients` (Block, Optional) Report recipients. Report recipients (see [below for nested schema](#nestedblock--report_recipients))
- `waap` (Block, Optional) Report Type Waap. Report Type Waap (see [below for nested schema](#nestedblock--waap))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--report_recipients"></a>
### Nested Schema for `report_recipients`

Optional:

- `user_groups` (Block List) User Groups. Select one or more user groups, to which the report should be sent via email ves.io.schema.rules.repeated.max_items: 5 ves.io.schema.rules.repeated.unique: true (see [below for nested schema](#nestedblock--report_recipients--user_groups))

<a id="nestedblock--report_recipients--user_groups"></a>
### Nested Schema for `report_recipients.user_groups`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name. Required: YES ves.io.schema.rules.message.required: true ves.io.schema.rules.string.max_bytes: 128 ves.io.schema.rules.string.min_bytes: 1
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace. ves.io.schema.rules.string.max_bytes: 64
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant. ves.io.schema.rules.string.max_bytes: 64



<a id="nestedblock--waap"></a>
### Nested Schema for `waap`

Optional:

- `current_namespace` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--waap--current_namespace))
- `daily` (Block, Optional) Report Frequency Daily. create report daily (see [below for nested schema](#nestedblock--waap--daily))
- `monthly` (Block, Optional) Report Frequency Monthly. create report monthly (see [below for nested schema](#nestedblock--waap--monthly))
- `namespaces` (Block, Optional) Namespaces. namespaces (see [below for nested schema](#nestedblock--waap--namespaces))
- `weekly` (Block, Optional) Report Frequency Weekly. create report weekly (see [below for nested schema](#nestedblock--waap--weekly))

<a id="nestedblock--waap--current_namespace"></a>
### Nested Schema for `waap.current_namespace`


<a id="nestedblock--waap--daily"></a>
### Nested Schema for `waap.daily`

Optional:

- `report_generation_time` (String) Report Generation Time. Times are in UTC time. Generating reports may be delayed up to 30 minutes from the time set. Required: YES ves.io.schema.rules.message.required: true ves.io.schema.rules.string.pattern: (((0[1-9])|(1[0-2])):([0-5])([0-9]):([0-5])([0-9])\s?(A|P)M)


<a id="nestedblock--waap--monthly"></a>
### Nested Schema for `waap.monthly`

Optional:

- `date` (String) Report Generation Date. report generation date Indicates field not being set Create report on Last day of month
- `report_generation_time` (String) Time Report is Generated. Times are in UTC time. Generating reports may be delayed up to 30 minutes from the time set. Required: YES ves.io.schema.rules.message.required: true ves.io.schema.rules.string.pattern: (((0[1-9])|(1[0-2])):([0-5])([0-9]):([0-5])([0-9])\s?(A|P)M)


<a id="nestedblock--waap--namespaces"></a>
### Nested Schema for `waap.namespaces`

Optional:

- `namespaces` (List of String) Namespaces. list of namespaces for which user wants to generate report Required: YES ves.io.schema.rules.message.required: true ves.io.schema.rules.repeated.max_items: 5 ves.io.schema.rules.repeated.unique: true


<a id="nestedblock--waap--weekly"></a>
### Nested Schema for `waap.weekly`

Optional:

- `day` (String) Report Generation Weekday. report generation weekday Indicates field not being set
- `report_generation_time` (String) Report Generation Time. Times are in UTC time. Generating reports may be delayed up to 30 minutes from the time set. Required: YES ves.io.schema.rules.message.required: true ves.io.schema.rules.string.pattern: (((0[1-9])|(1[0-2])):([0-5])([0-9]):([0-5])([0-9])\s?(A|P)M)

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_report_config.example system/example
```
